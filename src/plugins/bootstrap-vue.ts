import Vue from "vue";

// import { BootstrapVue, BootstrapVueIcons } from "bootstrap-vue";
const BootstrapVue = (window as any).BootstrapVue;
const BootstrapVueIcons = (window as any).BootstrapVueIcons;

Vue.use(BootstrapVue);
Vue.use(BootstrapVueIcons);
